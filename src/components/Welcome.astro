---
// No imports needed for this simple design
---

<div class="container">
	<main class="main">
		<div class="logo">
			<span class="logo-text">M</span>
		</div>
		<h1 class="heading">make.inc</h1>
		<h2 class="subheading">This is a starter template.</h2>
		<p class="description">Your AI agent is working and you should see changes soon.</p>
	</main>
</div>

<style>
	.container {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		background-color: #ffffff;
		font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
	}

	.main {
		text-align: center;
		max-width: 600px;
		padding: 2rem;
	}

	.logo {
		display: inline-flex;
		width: 64px;
		height: 64px;
		background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
		border-radius: 12px;
		align-items: center;
		justify-content: center;
		margin-bottom: 2rem;
		box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
	}

	.logo-text {
		color: white;
		font-size: 28px;
		font-weight: 700;
		letter-spacing: -0.5px;
	}

	.heading {
		font-size: 3rem;
		font-weight: 800;
		color: #111827;
		margin: 0 0 1rem 0;
		letter-spacing: -0.025em;
		line-height: 1.1;
	}

	.subheading {
		font-size: 1.5rem;
		font-weight: 600;
		color: #374151;
		margin: 0 0 1.5rem 0;
		line-height: 1.3;
	}

	.description {
		font-size: 1.125rem;
		color: #6b7280;
		margin: 0;
		line-height: 1.6;
		max-width: 500px;
		margin-left: auto;
		margin-right: auto;
	}

	@media (max-width: 768px) {
		.main {
			padding: 1rem;
		}

		.heading {
			font-size: 2.25rem;
		}

		.subheading {
			font-size: 1.25rem;
		}

		.description {
			font-size: 1rem;
		}

		.logo {
			width: 56px;
			height: 56px;
		}

		.logo-text {
			font-size: 24px;
		}
	}
</style>
